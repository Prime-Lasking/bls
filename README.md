# bls — the better ls command

A modern replacement for `ls` written in Rust.

## bls functionality
bls is still in beta which is why it has very little functionallity
bls when used gives a formatted and colorized filename and length in bytes of all the files in your current directory
